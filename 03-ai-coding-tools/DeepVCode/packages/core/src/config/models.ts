/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */

/**
 * ⚠️ 模型配置已迁移到服务端 - 客户端使用'auto'占位符
 *
 * 原因：实现客户端与模型完全解耦
 * 策略：所有模型选择由服务端内部决定，客户端传递'auto'让服务端决定
 * 迁移日期：根据《🟢客户端模型代码完全移除执行计划》
 *
 * 🎯 核心设计：
 * - 客户端代码保持不变（兼容性）
 * - 所有常量都指向'auto'
 * - 服务端收到'auto'请求时内部决定实际模型
 */

// ✅ 统一使用'auto'，让服务端内部决定具体模型
export const DEFAULT_GEMINI_MODEL = 'auto';
export const DEFAULT_GEMINI_FLASH_MODEL = 'auto';
export const DEFAULT_GEMINI_EMBEDDING_MODEL = 'auto';
