{
  "name": "deepv-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "frontend/packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter deepvki-web dev",
    "build": "pnpm --filter deepvki-web build",
    "start": "pnpm --filter deepvki-web start",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "cross-env": "^10.1.0"
  }
}